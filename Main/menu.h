#ifndef MENU_H
#define MENU_H
#include <iostream>
#include <curses.h>
#include <windows.h>
using namespace std;

class menu
{
    public:
        menu();
        virtual ~menu();
    protected:
    private:
};

#endif // MENU_H
